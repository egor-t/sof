<% if @answer.errors.present? %>
    $('.answer-errors').html("<%= show_error_messages(@answer) %>")
<% else %>
    $('[data-answer-id="' + "<%= @answer.id %>" + '"]').replaceWith("<%= j render @answer %>");
<% end %>

